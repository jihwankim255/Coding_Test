def solution(grid):
    # n = len(grid)
    # m = len(grid[0])
    # cur = [1,1]
    # cell = grid[cur[0]-1][cur[1]-1]
    
    # 첫점에서 (1,0) 이동했을 때

    #


    # 첫점에서 (0,1) 이동했을 때
    # 첫점에서 (-1,0) 이동했을 때
    # 첫점에서 (0,-1) 이동했을 때

    # 중복되면 제거
    
    # 정렬

    return 




# ["S"]	[1,1,1,1]
# ["R","R"]	[4,4]
# ["SL","LR"]	[16]

# 0,0 0, -1

print(solution(["S"]))
print(solution(["R","R"]))
print(solution(["SL","LR"]))